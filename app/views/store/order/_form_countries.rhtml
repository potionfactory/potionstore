<select id="countries" name="order[country]">
  	  <%=
  country = "US"
  country = @order.country if not @order.country.blank?

  options_for_select(country_name_and_code_pairs(), country) %>
	</select>

